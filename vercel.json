{
  "version": 2,
  "builds": [
    { "src": "api/backend/**/*.php", "use": "@vercel/php" },
    { "src": "api/**/*", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/api/backend/(.*)", "dest": "/api/backend/$1" },
    { "src": "/(.*)", "dest": "/api/$1" }
  ]
}
